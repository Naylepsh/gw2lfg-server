import { entities } from "./entities";
import { repositories } from "./repositories";
import { unitsOfWork } from "./units-of-work";

// Available repositories, uows and entities 
export const data = {
  repositories,
  unitsOfWork,
  entities
};
